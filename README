using twitterlib
	get a big bunch most recent tweets from twitter that are
		from @uxbri
		mention @uxbri
		are tagged #uxbri / #uxbrighton

	htmlify / prettify tweets - the pool
	
find container on page




										⎫
insert a tweet into top container		⎪
										⎬ the loop using spy technique
remove a tweet from bottom of container ⎪ get it updating at first then animate the updates when that works

wait a second

										⎭
										
when original pool to tweets is exhausted check for new tweets

add any new tweets to the pool

goto the loop


extend functionality with user defined options 
	for example: 	show hide display pics
					set number of tweets to initially fetch
					set number of tweets to show at a time
					etc.